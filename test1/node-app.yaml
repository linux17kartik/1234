#apiVersion: extensions/v1beta1
#kind: Deployment
#metadata:
#  name: iface-crm-api  #Name of the deployment
#spec:
 # replicas: 1
  #template:
   # metadata:
    #  labels:
     #   app: iface-crm-api
   # spec:
      #imagePullSecrets:
     #   - name: jenkinsmyregistrykey
    #  containers:
   #     - image: index.docker.io/linux17kartik/test:v1 
   #       name: web 
  #        imagePullPolicy: Always




